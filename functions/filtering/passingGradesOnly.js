const grades = [73, 69, 3, 100, 50, 70, 69, 88, 95, 77, 35];
// Filter the grades array to only include grades that are 70 or above
const passing = grades.filter(num => num >= 70);

console.log("These are the passing marks, looks like someone studied Bueller:",passing);

